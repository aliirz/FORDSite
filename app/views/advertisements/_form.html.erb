:html =>{:multipart =>true} 
This is used to define a MIME-multipart form (for file uploads) it looks like this 
<%= form_for(@advertisement, :html => { :multipart => true }) do |f| %> 
and also add 
<div> 
<%= f.label :pic %><br /> 
<%= f.file_field :pic %> 
</div> 
<%= form_for(@advertisement) do |f| %>
  <% if @advertisement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@advertisement.errors.count, "error") %> prohibited this advertisement from being saved:</h2>

      <ul>
      <% @advertisement.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
